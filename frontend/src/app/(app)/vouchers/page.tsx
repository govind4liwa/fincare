import { ComingSoon } from "@/components/coming-soon";

export default function VouchersPage() {
  return (
    <ComingSoon
      title="Vouchers"
      description="Receipt, payment, journal and contra vouchers with the double-entry posting flow. The backend API is already in place — this entry screen is an upcoming slice."
    />
  );
}
