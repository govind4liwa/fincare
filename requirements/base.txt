# ============================================================
# FinCare — Base requirements (shared dev + prod)
# Pin tight; update via dependabot only.
# ============================================================

# --- Core framework ---
Django==6.0.7
djangorestframework==3.17.1
django-environ==0.14.0

# --- Database ---
psycopg[binary]==3.3.4

# --- Auth ---
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0

# --- API schema ---
drf-spectacular==0.30.0

# --- Filters & utilities ---
django-filter==26.1
django-extensions==4.1
django-import-export==4.4.1

# --- Background tasks ---
celery==5.6.3
django-celery-beat==2.9.0
django-celery-results==2.6.0
redis==8.0.1

# --- File / Export generation ---
openpyxl==3.1.2
weasyprint==69.0
reportlab==4.2.0

# --- Money & numerics ---
django-money==3.6.1
py-moneyed==3.0

# --- Storage (S3-compatible) ---
django-storages==1.14.6
boto3==1.43.55

# --- Web server ---
gunicorn==22.0.0

# --- Validation / helpers ---
pydantic==2.7.3
python-dateutil==2.9.0.post0
pytz==2026.2

# --- Logging & observability ---
structlog==26.1.0
sentry-sdk==2.8.0

# --- Security ---
django-axes==8.3.1          # brute-force protection
django-csp==4.0             # content security policy
